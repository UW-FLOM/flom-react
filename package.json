{
  "scripts": {
    "setup": "yarn install && cd client && yarn install && cd ../api && yarn install",
    "db:setup": "cd api && yarn db:setup && cd ..",
    "db:start": "pg_ctl -D /usr/local/var/postgres start",
    "db:stop": "pg_ctl -D /usr/local/var/postgres stop",
    "prod:build": "cd client && yarn install && yarn build && cd ..",
    "prod:start": "cd api && yarn start:prod",
    "dev:setup": "yarn && cd client && yarn && cd ../api && yarn && docker-compose up -d",
    "dev:start" : "docker-compose -f docker-compose.dev.yml build && docker-compose -f docker-compose.dev.yml up -d",
    "dev:resume": "docker-compose -f docker-compose.dev.yml up -d",
    "dev:stop": "docker-compose stop",
    "dev:destroy": "docker-compose down -v"
  },
  "dependencies": {
    "concurrently": "^3.6.0"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "eslint": "^7.11.0",
    "eslint-config-airbnb": "^18.2.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-jsx-a11y": "^6.3.1",
    "eslint-plugin-react": "^7.21.4",
    "eslint-plugin-react-hooks": "^4.1.2"
  }
}
