{
  "scripts": {
    "setup": "yarn install && cd client && yarn install && cd ../server && yarn install",
    "client": "cd client && yarn install && yarn start",
    "server": "cd server && yarn install && yarn start:dev",
    "start:dev": "yarn psql:start && concurrently –kill-others-on-fail  \"yarn server\" \"yarn client\"",

    "start:prod": "cd server && yarn start:prod",
    "psql:start": "pg_ctl -D /usr/local/var/postgres start",
    "psql:stop": "pg_ctl -D /usr/local/var/postgres stop",
    "build:client": "cd client && yarn install && yarn build",
    "stop": "yarn psql:stop"
  },
  "dependencies": {
    "concurrently": "^3.6.0"
  },
  "license": "UNLICENSED"
}
